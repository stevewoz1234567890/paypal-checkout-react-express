# Backend base URL (no trailing slash), e.g. http://localhost:5000
REACT_APP_BASE_URL=http://localhost:5000

# Must match the PayPal app used by the backend (sandbox or live client ID)
REACT_APP_PAYPAL_CLIENT_ID=
